#in python we can import libraries for our usage in data science, ML etcetra

import math
print("the value of pi is ", math.pi)

import statistics
x = [100, 200, 600, 800]
print(statistics.mean(x))

import calendar
yy = 2020
mm = 11

yy = int(input("enter your year: "))
mm = int(input("enter your month: "))

print(calendar.month(yy, mm))

#there are many important libraries that we use in our advance classes